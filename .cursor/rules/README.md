# Cursor Rules для Apricode Exchange

Этот каталог содержит правила для настройки AI-ассистента Cursor под специфику проекта Apricode Exchange.

## 📁 Структура правил

```
.cursor/rules/
├── project.mdc      - Главные правила проекта (всегда применяются)
├── frontend.mdc     - Стандарты фронтенд разработки
├── api.mdc          - Стандарты API routes
├── database.mdc     - Работа с Prisma и БД
├── security.mdc     - Правила безопасности (всегда применяются)
└── testing.mdc      - Стандарты тестирования
```

## 🎯 Как это работает?

### Автоматическое применение

Cursor автоматически применяет правила на основе файла, с которым вы работаете:

- **project.mdc** и **security.mdc** - применяются **всегда** (`alwaysApply: true`)
- **frontend.mdc** - автоматически для файлов в `src/app/**/*.tsx`, `src/components/**/*.tsx`
- **api.mdc** - автоматически для файлов в `src/app/api/**/route.ts`
- **database.mdc** - автоматически для `prisma/**/*` и файлов с prisma
- **testing.mdc** - автоматически для `**/*.test.ts`, `**/*.test.tsx`

### Ручное применение

Вы можете явно подключить правило в промпте:

```
@Cursor Rules: frontend

Создай новый компонент для отображения списка заказов
```

## 📋 Содержание правил

### 1. project.mdc - Главные правила

- Описание проекта и его особенностей
- Tech stack и зависимости
- Общие принципы разработки
- TypeScript strict mode правила
- Именование файлов и импорты
- Бизнес-логика (KYC workflow, Order flow, Fees)

**Когда применяется:** Всегда

### 2. frontend.mdc - Frontend стандарты

- Server vs Client Components в Next.js
- Структура React компонентов
- Styling с Tailwind CSS
- Работа с формами (React Hook Form + Zod)
- UI feedback (toasts, loading, errors)
- shadcn/ui компоненты
- Navigation & routing
- Accessibility (A11y)

**Когда применяется:** При работе с `.tsx` файлами в `src/app` и `src/components`

### 3. api.mdc - API стандарты

- Структура API routes в Next.js 14
- Аутентификация и авторизация
- Валидация входных данных (Zod)
- HTTP response patterns
- Обработка ошибок
- Dynamic routes
- Внешние API вызовы (KYCAID, CoinGecko)
- Webhooks
- Security best practices

**Когда применяется:** При работе с файлами в `src/app/api`

### 4. database.mdc - Database & Prisma

- Prisma Client setup (singleton pattern)
- Database schema best practices
- Naming conventions
- Relations и indexes
- CRUD операции
- Filtering, sorting, pagination
- Transactions
- Aggregations
- Soft delete pattern
- Performance optimization
- Migrations

**Когда применяется:** При работе с `prisma/schema.prisma` и БД запросами

### 5. security.mdc - Безопасность

- Environment variables
- Password hashing
- Session management
- Input validation
- Authorization
- Webhook signature verification
- Rate limiting
- Sensitive data handling
- CORS & CSP
- Error handling
- Security checklist

**Когда применяется:** Всегда (критически важно)

### 6. testing.mdc - Тестирование

- Test-Driven Development (TDD)
- Unit tests
- Integration tests
- Component tests
- E2E tests
- Mocking
- Test coverage
- Best practices
- Антипаттерны

**Когда применяется:** При работе с тестовыми файлами

## 🚀 Примеры использования

### Пример 1: Создание нового компонента

Когда вы работаете с файлом в `src/components/`, автоматически применяются:
- ✅ project.mdc (общие правила)
- ✅ frontend.mdc (React/Next.js стандарты)
- ✅ security.mdc (безопасность)

Промпт:
```
Создай компонент OrderCard для отображения информации о заказе.
Должен показывать: валюту, сумму, статус, дату создания.
```

Cursor будет следовать всем правилам фронтенда: использовать TypeScript, shadcn/ui компоненты, Tailwind CSS, etc.

### Пример 2: Создание API endpoint

Когда вы работаете с `src/app/api/orders/route.ts`, применяются:
- ✅ project.mdc
- ✅ api.mdc (API стандарты)
- ✅ security.mdc

Промпт:
```
Создай GET endpoint для получения списка заказов пользователя.
Добавь фильтрацию по статусу и пагинацию.
```

Cursor автоматически добавит:
- Проверку аутентификации
- Валидацию входных данных
- Правильные HTTP статус коды
- Error handling
- TypeScript типы

### Пример 3: Работа с базой данных

При работе с Prisma queries:
- ✅ project.mdc
- ✅ database.mdc
- ✅ security.mdc

Промпт:
```
Создай функцию для получения всех активных заказов пользователя
с подгрузкой связанных данных о валюте.
```

Cursor будет использовать:
- Правильный Prisma client (singleton)
- Select/Include правильно
- Indexes для производительности
- Transaction где нужно

### Пример 4: Написание тестов

При работе с `.test.ts` файлами:
- ✅ project.mdc
- ✅ testing.mdc
- ✅ security.mdc

Промпт:
```
Напиши тесты для API endpoint POST /api/orders.
Покрой все edge cases и проверку авторизации.
```

Cursor создаст:
- Unit tests с моками
- Arrange-Act-Assert структуру
- Описательные названия тестов
- Edge cases

## 🔧 Настройка

### Обновление правил

Просто отредактируй нужный `.mdc` файл и сохрани. Cursor автоматически подхватит изменения.

### Добавление новых правил

1. Создай новый `.mdc` файл в `.cursor/rules/`
2. Добавь метаданные в начале:

```markdown
---
description: "Описание правила"
globs: ["путь/к/файлам/**/*"]
---

# Содержание правила
...
```

3. Сохрани файл

### Отключение правила

Временно отключить можно, изменив `alwaysApply: false` или убрав `globs`.

## 📊 Метаданные правил

### alwaysApply

```yaml
alwaysApply: true  # Правило применяется всегда
alwaysApply: false # Правило не применяется автоматически
```

### globs

```yaml
globs: 
  - "src/app/**/*.tsx"        # Все .tsx в src/app
  - "src/components/**/*"     # Всё в components
  - "**/*.test.ts"            # Все тестовые файлы
```

### Поддерживаемые шаблоны:

- `**/*` - все файлы во всех подпапках
- `*.tsx` - файлы с расширением .tsx
- `src/app/**/route.ts` - все route.ts в любой вложенности

## 🎓 Best Practices

1. **Держи правила актуальными** - обновляй при изменении требований проекта
2. **Будь конкретным** - чем точнее правила, тем лучше результат
3. **Добавляй примеры кода** - AI лучше учится на примерах
4. **Не перегружай** - лучше несколько целевых правил, чем один огромный файл
5. **Версионируй** - храни правила в Git вместе с кодом
6. **Проверяй** - периодически проверяй, следует ли Cursor правилам

## 🚨 Важные замечания

- Правила **НЕ гарантируют** 100% соблюдения - всегда проверяй сгенерированный код
- Большие правила (>500 строк) могут снижать эффективность
- Конфликтующие правила могут давать непредсказуемый результат
- Security правила критически важны - никогда не игнорируй их

## 📚 Дополнительные ресурсы

- [Cursor Documentation](https://docs.cursor.com)
- [Cursor Rules Examples](https://cursor.directory)
- [Apricode Exchange Project Spec](../README.md)

## 🤝 Контрибьюция

При добавлении новых правил:

1. Следуй существующему формату
2. Добавь примеры кода
3. Документируй метаданные
4. Тестируй на реальных сценариях
5. Обнови этот README

---

**Правила созданы для проекта Apricode Exchange - Cryptocurrency Exchange Platform**
