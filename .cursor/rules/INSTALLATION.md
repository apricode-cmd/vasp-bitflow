# 🚀 Установка Cursor Rules для Apricode Exchange

## Быстрый старт

### Шаг 1: Скопируй файлы в проект

```bash
# В корне твоего проекта apricode-exchange/

# Создай директорию для правил
mkdir -p .cursor/rules

# Скопируй все файлы правил
cp path/to/downloaded/rules/* .cursor/rules/

# Скопируй .cursorignore в корень проекта
cp path/to/downloaded/.cursorignore .cursorignore
```

### Шаг 2: Структура должна выглядеть так

```
apricode-exchange/
├── .cursor/
│   └── rules/
│       ├── README.md
│       ├── project.mdc
│       ├── frontend.mdc
│       ├── api.mdc
│       ├── database.mdc
│       ├── security.mdc
│       └── testing.mdc
├── .cursorignore
├── src/
├── prisma/
├── package.json
└── ...
```

### Шаг 3: Перезапусти Cursor

Закрой и открой Cursor IDE заново, чтобы правила загрузились.

### Шаг 4: Проверь работу

Открой любой файл, например `src/app/api/orders/route.ts`, и попроси Cursor:

```
Создай GET endpoint для получения заказов с фильтрацией по статусу
```

Cursor должен автоматически применить правила из `api.mdc` и `security.mdc`.

## 📋 Что делают эти правила?

### 1. Автоматическая настройка под проект

Cursor теперь "знает" о:
- ✅ Tech stack (Next.js 14, TypeScript, Prisma, NextAuth)
- ✅ Архитектуре проекта
- ✅ Бизнес-логике (KYC workflow, Order flow)
- ✅ Стандартах кодирования
- ✅ Требованиях безопасности

### 2. Умное применение правил

Cursor автоматически применяет нужные правила в зависимости от файла:

| Файл | Применяемые правила |
|------|---------------------|
| `src/components/OrderCard.tsx` | project + frontend + security |
| `src/app/api/orders/route.ts` | project + api + security |
| `prisma/schema.prisma` | project + database + security |
| `src/lib/auth.test.ts` | project + testing + security |

### 3. Консистентный код

Все члены команды получают одинаковые рекомендации от AI.

## 🎯 Примеры использования

### Пример 1: Создание компонента

**Промпт:**
```
Создай компонент для отображения карточки заказа.
Покажи валюту, сумму, статус с цветовой индикацией.
```

**Результат:**
- ✅ TypeScript с правильными типами
- ✅ shadcn/ui компоненты
- ✅ Tailwind CSS классы
- ✅ Responsive дизайн
- ✅ Accessibility (aria-labels)

### Пример 2: API Endpoint

**Промпт:**
```
Создай POST /api/orders endpoint для создания заказа.
Проверь KYC статус пользователя.
```

**Результат:**
- ✅ Проверка аутентификации
- ✅ Валидация Zod схемой
- ✅ Проверка KYC перед созданием
- ✅ Правильные HTTP статусы
- ✅ Error handling
- ✅ Security best practices

### Пример 3: Database Query

**Промпт:**
```
Напиши функцию для получения всех заказов пользователя
с пагинацией и сортировкой по дате.
```

**Результат:**
- ✅ Singleton Prisma client
- ✅ Правильный select для performance
- ✅ Pagination с skip/take
- ✅ OrderBy с типами
- ✅ Error handling

### Пример 4: Тесты

**Промпт:**
```
Напиши тесты для функции calculateOrderTotal
```

**Результат:**
- ✅ Arrange-Act-Assert структура
- ✅ Описательные названия
- ✅ Edge cases покрыты
- ✅ Правильные assertions

## 🔧 Кастомизация правил

### Изменение существующих правил

Отредактируй нужный `.mdc` файл:

```bash
# Например, добавить новый стиль компонента
nano .cursor/rules/frontend.mdc
```

Добавь свои правила в соответствующий раздел, сохрани - Cursor применит изменения автоматически.

### Добавление новых правил

Создай новый файл:

```bash
nano .cursor/rules/custom-feature.mdc
```

Добавь метаданные:

```markdown
---
description: "Мои кастомные правила"
globs: ["src/features/myfeature/**/*"]
---

# Твои правила здесь
```

### Временное отключение правила

Измени `alwaysApply` на `false` или закомментируй `globs`:

```yaml
---
description: "Frontend rules"
# globs: ["src/app/**/*.tsx"]  # Закомментировано
---
```

## 🎓 Best Practices

### 1. Регулярно обновляй правила

Когда меняется архитектура или добавляются новые паттерны - обновляй правила.

### 2. Коммить в Git

```bash
git add .cursor/rules/
git commit -m "feat: add cursor rules for project"
```

Все в команде получат одинаковые правила.

### 3. Проверяй результат

AI следует правилам, но **не слепо доверяй**. Всегда проверяй:
- ✅ Типы TypeScript
- ✅ Security (нет ли секретов в коде)
- ✅ Логика работает корректно
- ✅ Тесты проходят

### 4. Используй с TDD

Комбинируй правила с Test-Driven Development:

```
Напиши тесты для функции создания заказа, затем реализацию
```

Cursor создаст тесты следуя `testing.mdc`, а реализацию - следуя остальным правилам.

## 🚨 Частые проблемы

### Правила не применяются

**Решение:**
1. Перезапусти Cursor
2. Проверь, что файлы в правильной директории `.cursor/rules/`
3. Проверь синтаксис YAML метаданных
4. Проверь, что `globs` соответствуют файлу

### Cursor игнорирует некоторые правила

**Причины:**
- Слишком много правил (>500 строк в одном файле)
- Конфликтующие инструкции
- Правило не применимо к контексту

**Решение:**
- Разбей большие файлы на меньшие
- Уточни `globs` для более точного применения

### Cursor предлагает небезопасный код

**Решение:**
- Проверь, что `security.mdc` имеет `alwaysApply: true`
- Усиль формулировки в security правилах
- Явно упомяни в промпте: "соблюдай security правила"

## 📊 Мониторинг эффективности

### Проверяй регулярно:

1. **Качество кода**
   - Cursor соблюдает TypeScript типы?
   - Используются правильные паттерны?

2. **Безопасность**
   - Нет ли hardcoded секретов?
   - Валидация везде присутствует?

3. **Консистентность**
   - Код от разных разработчиков похож?
   - Стиль единообразный?

## 🎉 Готово!

Теперь Cursor настроен под Apricode Exchange. Начинай разработку:

```
Создай страницу /buy для формы покупки криптовалюты
```

Cursor автоматически применит все нужные правила и создаст код, соответствующий стандартам проекта.

## 📚 Дополнительная информация

- [Подробный README](.cursor/rules/README.md)
- [Cursor Docs](https://docs.cursor.com)
- [Project Specification](../project-spec.txt)

## 🆘 Поддержка

Если правила не работают как ожидается:

1. Проверь структуру файлов
2. Перезапусти Cursor
3. Проверь логи (View > Toggle Developer Tools)
4. Уточни формулировки в правилах

---

**Happy coding with AI! 🚀**
