---
description: "Database и Prisma стандарты"
globs: ["prisma/**/*", "src/lib/prisma.ts", "**/*prisma*"]
---

# Database & Prisma Standards - Apricode Exchange

## 🗄️ Prisma Client Setup

### Singleton Pattern (обязательно)

```typescript
// src/lib/prisma.ts

import { PrismaClient } from '@prisma/client';

const globalForPrisma = globalThis as unknown as {
  prisma: PrismaClient | undefined;
};

export const prisma =
  globalForPrisma.prisma ??
  new PrismaClient({
    log: process.env.NODE_ENV === 'development' 
      ? ['query', 'error', 'warn'] 
      : ['error'],
  });

if (process.env.NODE_ENV !== 'production') {
  globalForPrisma.prisma = prisma;
}

// ✅ Всегда импортируй так:
// import { prisma } from '@/lib/prisma';
```

### Почему singleton важен?

```typescript
// ❌ НЕ создавай новые инстансы
const prisma = new PrismaClient(); // Каждый раз новое подключение!

// ✅ Используй singleton
import { prisma } from '@/lib/prisma'; // Переиспользование подключения
```

## 📋 Database Schema Best Practices

### Naming Conventions

```prisma
// ✅ Правильно
model User {
  id        String   @id @default(cuid())
  email     String   @unique
  firstName String?  @map("first_name")  // snake_case в БД
  
  orders    Order[]  // Множественное число для relations
  profile   Profile? // Единственное для one-to-one
  
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")
  
  @@index([email])
  @@map("users") // Таблица в БД в snake_case и множественном числе
}

// ❌ Неправильно
model user {  // lowercase
  ID String   // UPPERCASE
  Order Order[] // Единственное число
}
```

### ID Generation

```prisma
// ✅ Используй cuid для ID
id String @id @default(cuid())

// ❌ НЕ используй auto-increment для распределённых систем
id Int @id @default(autoincrement())

// ✅ Для UUID (если предпочитаешь)
id String @id @default(uuid())
```

### Timestamps (всегда включай)

```prisma
model Order {
  id String @id @default(cuid())
  
  // ... другие поля
  
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  @@map("orders")
}
```

### Indexes для производительности

```prisma
model Order {
  id     String @id @default(cuid())
  userId String
  status OrderStatus
  
  user User @relation(fields: [userId], references: [id])
  
  // ✅ Index на foreign keys
  @@index([userId])
  
  // ✅ Index на часто используемые фильтры
  @@index([status])
  
  // ✅ Composite index для частых комбинаций
  @@index([userId, status])
  
  // ✅ Уникальный индекс
  @@unique([referenceId])
}
```

### Enums

```prisma
// ✅ Используй enums для фиксированных значений
enum Role {
  CLIENT
  ADMIN
}

enum OrderStatus {
  PENDING
  PROCESSING
  COMPLETED
  CANCELLED
  FAILED
}

enum KycStatus {
  PENDING
  APPROVED
  REJECTED
}

model User {
  role Role @default(CLIENT)
}
```

### Relations

```prisma
// ✅ One-to-Many
model User {
  id     String  @id @default(cuid())
  orders Order[] // Множественное число
}

model Order {
  id     String @id @default(cuid())
  userId String
  user   User   @relation(fields: [userId], references: [id])
  
  @@index([userId])
}

// ✅ One-to-One
model User {
  id      String   @id @default(cuid())
  profile Profile?
}

model Profile {
  id     String @id @default(cuid())
  userId String @unique
  user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)
}

// ✅ Many-to-Many (через join table)
model User {
  id       String         @id @default(cuid())
  favorites UserFavorite[]
}

model Currency {
  id        String         @id @default(cuid())
  favorites UserFavorite[]
}

model UserFavorite {
  userId     String
  currencyId String
  user       User     @relation(fields: [userId], references: [id])
  currency   Currency @relation(fields: [currencyId], references: [id])
  
  @@id([userId, currencyId])
}
```

### Cascade Delete

```prisma
// ✅ Используй onDelete для дочерних записей
model User {
  id      String   @id
  profile Profile?
  orders  Order[]
}

model Profile {
  userId String @unique
  user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)
}

// При удалении User -> автоматически удалится Profile
```

## 🔍 Querying Patterns

### Basic CRUD

```typescript
// ✅ Create
const user = await prisma.user.create({
  data: {
    email: 'user@example.com',
    password: hashedPassword,
    profile: {
      create: {
        firstName: 'John',
        lastName: 'Doe'
      }
    }
  },
  include: {
    profile: true
  }
});

// ✅ Read
const user = await prisma.user.findUnique({
  where: { id: userId },
  select: {
    id: true,
    email: true,
    profile: {
      select: {
        firstName: true,
        lastName: true
      }
    }
  }
});

// ✅ Update
const updatedOrder = await prisma.order.update({
  where: { id: orderId },
  data: {
    status: 'COMPLETED',
    completedAt: new Date()
  }
});

// ✅ Delete
await prisma.order.delete({
  where: { id: orderId }
});
```

### Filtering

```typescript
// ✅ Простые фильтры
const pendingOrders = await prisma.order.findMany({
  where: {
    status: 'PENDING',
    userId: currentUserId
  }
});

// ✅ Множественные условия (AND)
const filteredOrders = await prisma.order.findMany({
  where: {
    AND: [
      { status: 'PENDING' },
      { amount: { gte: 100 } }
    ]
  }
});

// ✅ OR условия
const orders = await prisma.order.findMany({
  where: {
    OR: [
      { status: 'PENDING' },
      { status: 'PROCESSING' }
    ]
  }
});

// ✅ Вложенные фильтры
const users = await prisma.user.findMany({
  where: {
    kycSession: {
      status: 'APPROVED'
    }
  }
});

// ✅ NOT условия
const nonCompletedOrders = await prisma.order.findMany({
  where: {
    NOT: {
      status: 'COMPLETED'
    }
  }
});
```

### Sorting & Pagination

```typescript
// ✅ Sorting
const orders = await prisma.order.findMany({
  orderBy: {
    createdAt: 'desc' // или 'asc'
  }
});

// ✅ Multiple sorting
const orders = await prisma.order.findMany({
  orderBy: [
    { status: 'asc' },
    { createdAt: 'desc' }
  ]
});

// ✅ Pagination
const page = 1;
const limit = 10;

const orders = await prisma.order.findMany({
  skip: (page - 1) * limit,
  take: limit,
  orderBy: { createdAt: 'desc' }
});

// ✅ Count для total pages
const total = await prisma.order.count({
  where: { status: 'PENDING' }
});

const totalPages = Math.ceil(total / limit);
```

### Select vs Include

```typescript
// ✅ Select - выбери только нужные поля (лучше для производительности)
const user = await prisma.user.findUnique({
  where: { id: userId },
  select: {
    id: true,
    email: true,
    profile: {
      select: {
        firstName: true,
        lastName: true
      }
    }
  }
});

// ✅ Include - включи связанные данные
const order = await prisma.order.findUnique({
  where: { id: orderId },
  include: {
    user: true,
    currency: true
  }
});

// ❌ НЕ используй оба одновременно
const data = await prisma.user.findUnique({
  select: { id: true },
  include: { orders: true } // Ошибка!
});
```

## 🔄 Transactions

### Простая транзакция

```typescript
// ✅ Используй для операций, которые должны быть атомарными
const result = await prisma.$transaction(async (tx) => {
  // Обнови заказ
  const order = await tx.order.update({
    where: { id: orderId },
    data: { status: 'COMPLETED' }
  });

  // Создай audit log
  await tx.auditLog.create({
    data: {
      action: 'ORDER_COMPLETED',
      userId: order.userId,
      details: { orderId: order.id }
    }
  });

  // Отправь email
  await sendEmail({
    to: order.user.email,
    subject: 'Order completed'
  });

  return order;
});
```

### Последовательные транзакции

```typescript
// ✅ Для нескольких независимых операций
const [orders, users] = await prisma.$transaction([
  prisma.order.findMany(),
  prisma.user.findMany()
]);
```

### Обработка ошибок в транзакциях

```typescript
try {
  await prisma.$transaction(async (tx) => {
    // Операции
  });
} catch (error) {
  // Все изменения откатятся автоматически
  console.error('Transaction failed:', error);
  throw error;
}
```

## 🔢 Aggregations

```typescript
// ✅ Count
const orderCount = await prisma.order.count({
  where: { status: 'COMPLETED' }
});

// ✅ Aggregate
const stats = await prisma.order.aggregate({
  where: { status: 'COMPLETED' },
  _sum: {
    amount: true
  },
  _avg: {
    amount: true
  },
  _min: {
    createdAt: true
  },
  _max: {
    createdAt: true
  }
});

// ✅ Group By
const ordersByStatus = await prisma.order.groupBy({
  by: ['status'],
  _count: {
    id: true
  }
});

// Результат: [{ status: 'PENDING', _count: { id: 5 } }, ...]
```

## 🔐 Soft Delete Pattern

```prisma
// Добавь поле deletedAt
model Order {
  id        String    @id @default(cuid())
  deletedAt DateTime?
  
  @@map("orders")
}
```

```typescript
// ✅ Soft delete
await prisma.order.update({
  where: { id: orderId },
  data: { deletedAt: new Date() }
});

// ✅ Exclude deleted в запросах
const activeOrders = await prisma.order.findMany({
  where: {
    deletedAt: null
  }
});

// ✅ Или создай middleware для автоматической фильтрации
prisma.$use(async (params, next) => {
  if (params.model === 'Order') {
    if (params.action === 'findMany' || params.action === 'findFirst') {
      params.args.where = {
        ...params.args.where,
        deletedAt: null
      };
    }
  }
  return next(params);
});
```

## 🚀 Performance Optimization

### 1. Используй индексы

```prisma
model Order {
  userId String
  status OrderStatus
  
  // ✅ Индекс на часто фильтруемые поля
  @@index([userId])
  @@index([status])
  @@index([userId, status]) // Composite для частых комбинаций
}
```

### 2. Select только нужные поля

```typescript
// ❌ Медленно - загружает все поля
const user = await prisma.user.findUnique({
  where: { id: userId }
});

// ✅ Быстро - только нужные поля
const user = await prisma.user.findUnique({
  where: { id: userId },
  select: {
    id: true,
    email: true
  }
});
```

### 3. Batch операции

```typescript
// ❌ Медленно - N запросов
for (const order of orders) {
  await prisma.order.update({
    where: { id: order.id },
    data: { status: 'COMPLETED' }
  });
}

// ✅ Быстро - 1 запрос
await prisma.order.updateMany({
  where: {
    id: { in: orders.map(o => o.id) }
  },
  data: { status: 'COMPLETED' }
});
```

### 4. Используй cursor-based pagination для больших датасетов

```typescript
// ✅ Cursor pagination (эффективнее для больших данных)
const orders = await prisma.order.findMany({
  take: 10,
  cursor: lastOrderId ? { id: lastOrderId } : undefined,
  skip: lastOrderId ? 1 : 0,
  orderBy: { createdAt: 'desc' }
});
```

## 🏗️ Migrations

### Создание миграций

```bash
# ✅ Development
npx prisma migrate dev --name add_kyc_session

# ✅ Production
npx prisma migrate deploy

# ✅ Reset database (ОСТОРОЖНО!)
npx prisma migrate reset
```

### Best Practices для миграций

1. **Называй понятно:** `add_user_role`, `update_order_status_enum`
2. **Проверяй перед push:** Всегда тестируй миграции локально
3. **Не редактируй существующие:** Создавай новую миграцию для изменений
4. **Backup перед production:** Всегда делай бэкап перед применением миграций

### Seed данные

```typescript
// prisma/seed.ts

import { PrismaClient } from '@prisma/client';
import bcrypt from 'bcryptjs';

const prisma = new PrismaClient();

async function main() {
  // ✅ Создай админа
  const adminPassword = await bcrypt.hash('Admin123!', 10);
  
  const admin = await prisma.user.upsert({
    where: { email: 'admin@apricode.io' },
    update: {},
    create: {
      email: 'admin@apricode.io',
      password: adminPassword,
      role: 'ADMIN',
      profile: {
        create: {
          firstName: 'Admin',
          lastName: 'User'
        }
      }
    }
  });

  // ✅ Создай валюты
  const currencies = ['BTC', 'ETH', 'USDT', 'SOL'];
  
  for (const code of currencies) {
    await prisma.currency.upsert({
      where: { code },
      update: {},
      create: {
        code,
        name: code,
        isActive: true
      }
    });
  }

  console.log('✅ Seed completed');
}

main()
  .catch((e) => {
    console.error(e);
    process.exit(1);
  })
  .finally(async () => {
    await prisma.$disconnect();
  });
```

## 🐛 Error Handling

### Prisma Error Codes

```typescript
import { Prisma } from '@prisma/client';

try {
  await prisma.user.create({ data });
} catch (error) {
  if (error instanceof Prisma.PrismaClientKnownRequestError) {
    // ✅ Unique constraint violation
    if (error.code === 'P2002') {
      return { error: 'Email already exists' };
    }
    
    // ✅ Record not found
    if (error.code === 'P2025') {
      return { error: 'Record not found' };
    }
    
    // ✅ Foreign key constraint
    if (error.code === 'P2003') {
      return { error: 'Related record not found' };
    }
  }
  
  throw error;
}
```

### Common Error Codes

- `P2002` - Unique constraint violation
- `P2003` - Foreign key constraint failed
- `P2025` - Record not found
- `P1001` - Can't reach database
- `P1008` - Operations timed out

## 🚫 Антипаттерны (НЕ делай так)

```typescript
// ❌ N+1 Query Problem
const users = await prisma.user.findMany();
for (const user of users) {
  user.orders = await prisma.order.findMany({
    where: { userId: user.id }
  }); // N запросов!
}

// ✅ Используй include
const users = await prisma.user.findMany({
  include: { orders: true } // 1 запрос
});

// ❌ Не используй raw queries без необходимости
await prisma.$executeRaw`UPDATE users SET email = ${email}`;

// ✅ Используй Prisma API
await prisma.user.update({
  where: { id },
  data: { email }
});

// ❌ Не создавай новые клиенты
const db = new PrismaClient();

// ✅ Используй singleton
import { prisma } from '@/lib/prisma';
```

## 📊 Prisma Studio

```bash
# ✅ Открой GUI для БД
npx prisma studio

# Откроется на http://localhost:5555
# Можно просматривать и редактировать данные
```

---

**Помни:** База данных - критичный компонент. Всегда используй транзакции для связанных операций и индексы для производительности.
