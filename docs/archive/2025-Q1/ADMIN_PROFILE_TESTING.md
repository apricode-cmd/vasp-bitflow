# 🧪 Testing Guide - Admin Profile & Security

## 🎯 Цель тестирования

Проверить новый функционал Admin Profile:
1. **Passkey Management** - регистрация и удаление Passkeys
2. **Advanced Security Settings** - расширенные настройки безопасности
3. **Session Manager** - управление активными сессиями

---

## 📋 Предварительные требования

### 1. Убедитесь, что сервер запущен:
```bash
npm run dev
```
Должен быть доступен на `http://localhost:3000`

### 2. У вас есть админ аккаунт с Passkey:
- Email: `admin@apricode.io` (или `test@apricode.io`)
- Passkey: уже зарегистрирован

### 3. Проверьте браузер:
- Используйте современный браузер (Chrome, Safari, Edge)
- WebAuthn должен быть поддержан
- Touch ID / Face ID настроен (для Mac/iPhone)

---

## ✅ Тест #1: Passkey Management

### Шаги:

**1. Войдите в админку:**
```
http://localhost:3000/admin/auth/login
```
- Введите email
- Используйте Touch ID для входа

**2. Откройте профиль:**
```
http://localhost:3000/admin/profile
```

**3. Перейдите на вкладку "Security"**

**4. Найдите секцию "Passkeys (WebAuthn)"**

### Проверки:

#### ✓ Отображение существующих Passkeys:
- [ ] Видны все зарегистрированные Passkeys
- [ ] Отображается имя устройства
- [ ] Показан тип устройства (platform/cross-platform)
- [ ] Видна дата создания
- [ ] Видна дата последнего использования (если есть)
- [ ] Есть кнопка удаления (корзина)

#### ✓ Регистрация нового Passkey:
**Шаги:**
1. Нажмите "Add Passkey"
2. Введите имя устройства (например, "MacBook Pro M3")
3. Нажмите "Register Passkey"
4. Подтвердите Touch ID

**Ожидаемый результат:**
- [ ] Открылся диалог Touch ID / Face ID
- [ ] После подтверждения показано сообщение "Passkey registered successfully!"
- [ ] Диалог закрылся
- [ ] Новый Passkey появился в списке
- [ ] В консоли браузера нет ошибок

**Проверка в БД:**
```sql
SELECT * FROM "WebAuthnCredential" 
WHERE "adminId" = 'YOUR_ADMIN_ID' 
ORDER BY "createdAt" DESC LIMIT 1;
```
Должна появиться новая запись.

#### ✓ Удаление Passkey:
**Условие:** У вас должно быть минимум 2 Passkey

**Шаги:**
1. Нажмите кнопку корзины у одного из Passkeys
2. Подтвердите удаление

**Ожидаемый результат:**
- [ ] Показан диалог подтверждения
- [ ] После подтверждения: "Passkey removed successfully"
- [ ] Passkey исчез из списка

**Проверка последнего Passkey:**
1. Попробуйте удалить последний Passkey
2. Должно быть сообщение: "Cannot remove last passkey. Add another passkey first."

---

## ✅ Тест #2: Advanced Security Settings

### Шаги:

**1. На вкладке "Security" прокрутите до "Security Settings"**

**2. Найдите поле "Session Timeout"**

### Проверки:

#### ✓ Session Timeout:
- [ ] Dropdown с вариантами: 15min, 30min, 1h, 2h, 8h, 24h
- [ ] Можно выбрать любой вариант
- [ ] Дефолтное значение: 30 минут

#### ✓ Login Notifications:
- [ ] Есть переключатель (Switch)
- [ ] Можно включить/выключить
- [ ] Дефолтное значение: включено (ON)

#### ✓ Сохранение настроек:
**Шаги:**
1. Измените Session Timeout на "15 minutes"
2. Включите Login Notifications (если выключены)
3. Нажмите "Save Settings"

**Ожидаемый результат:**
- [ ] Показано сообщение: "Security settings updated successfully"
- [ ] Настройки сохранены

**Проверка в БД:**
```sql
SELECT * FROM "AdminSettings" 
WHERE "adminId" = 'YOUR_ADMIN_ID';
```
Должны обновиться значения:
- `sessionTimeout` = 15
- `loginNotifications` = true

**Проверка в AuditLog:**
```sql
SELECT * FROM "AuditLog" 
WHERE "adminId" = 'YOUR_ADMIN_ID' 
  AND action = 'SETTINGS_UPDATED'
ORDER BY "createdAt" DESC LIMIT 1;
```
Должна быть запись о изменении настроек.

---

## ✅ Тест #3: Session Manager

### Шаги:

**1. Перейдите на вкладку "Sessions"**

### Проверки:

#### ✓ Текущая сессия:
- [ ] Видна одна сессия с меткой "Current Session"
- [ ] Показано устройство (например, "Unknown Device")
- [ ] Показан браузер и ОС
- [ ] Показана дата последней активности
- [ ] Нет кнопки "Terminate" для текущей сессии

#### ✓ Создание второй сессии:
**Шаги:**
1. Откройте Chrome в режиме инкогнито (⌘+Shift+N)
2. Перейдите на `http://localhost:3000/admin/auth/login`
3. Войдите с Passkey
4. В основном окне обновите вкладку "Sessions"

**Ожидаемый результат:**
- [ ] Теперь видны 2 сессии
- [ ] Одна помечена "Current Session"
- [ ] У второй есть кнопка "Terminate"

#### ✓ Завершение сессии:
**Шаги:**
1. Нажмите "Terminate" у второй сессии

**Ожидаемый результат:**
- [ ] Показано сообщение: "Session terminated"
- [ ] Сессия исчезла из списка
- [ ] В инкогнито окне при обновлении страницы редирект на `/admin/auth/login`

**Проверка в БД:**
```sql
SELECT * FROM "AdminSession" 
WHERE "adminId" = 'YOUR_ADMIN_ID' 
  AND "isValid" = false
ORDER BY "terminatedAt" DESC LIMIT 1;
```
Должна быть запись с:
- `isValid` = false
- `terminatedAt` = текущее время
- `terminationReason` = 'USER_REVOKED'

---

## 🐛 Что проверять на баги:

### UI/UX:
- [ ] Все кнопки кликабельны
- [ ] Диалоги открываются и закрываются
- [ ] Формы валидируются корректно
- [ ] Toast уведомления появляются
- [ ] Loader показывается во время загрузки
- [ ] Нет layout shifts при загрузке данных

### Безопасность:
- [ ] Нельзя удалить последний Passkey
- [ ] Нельзя завершить текущую сессию
- [ ] API проверяет владение ресурсами
- [ ] Все действия логируются в AuditLog

### Производительность:
- [ ] Страница загружается быстро (<2 секунд)
- [ ] API запросы не дублируются
- [ ] Нет лишних ререндеров в консоли

### Браузерная совместимость:
- [ ] Chrome (Passkey через Touch ID)
- [ ] Safari (Passkey через Touch ID)
- [ ] Edge (Passkey через Windows Hello)

---

## 📊 Ожидаемые результаты:

### После всех тестов:

**В базе данных:**
```sql
-- Должно быть 2+ Passkeys
SELECT COUNT(*) FROM "WebAuthnCredential" 
WHERE "adminId" = 'YOUR_ADMIN_ID' AND "isActive" = true;

-- Должна быть запись в AdminSettings
SELECT * FROM "AdminSettings" WHERE "adminId" = 'YOUR_ADMIN_ID';

-- Должны быть записи в AuditLog
SELECT COUNT(*) FROM "AuditLog" 
WHERE "adminId" = 'YOUR_ADMIN_ID' 
  AND action IN ('PASSKEY_REMOVED', 'SETTINGS_UPDATED', 'SESSION_TERMINATED');
```

**В UI:**
- Все вкладки работают
- Passkey Management функционален
- Security Settings сохраняются
- Session Manager отображает сессии

---

## 🚨 Известные ограничения:

1. **Device Name** - вводится вручную, нет автоопределения
2. **Location** - пока не заполняется (требует GeoIP API)
3. **Idle Timeout** - пока не реализован (требует middleware)
4. **IP Whitelist** - настройка есть, но UI для управления нет

Эти ограничения не критичны для MVP и могут быть добавлены позже.

---

## ✅ Checklist для QA:

- [ ] Passkey Management: регистрация работает
- [ ] Passkey Management: удаление работает
- [ ] Passkey Management: защита последнего ключа работает
- [ ] Security Settings: сохранение работает
- [ ] Security Settings: значения корректны
- [ ] Session Manager: отображение сессий работает
- [ ] Session Manager: завершение сессии работает
- [ ] Session Manager: текущая сессия защищена
- [ ] Все действия логируются в AuditLog
- [ ] Нет ошибок в консоли браузера
- [ ] Нет ошибок в логах сервера

---

## 📞 Поддержка:

Если нашли баг:
1. Скопируйте ошибку из консоли браузера
2. Скопируйте ошибку из терминала (если есть)
3. Опишите шаги для воспроизведения
4. Приложите скриншот (если возможно)

**Удачного тестирования!** 🚀

