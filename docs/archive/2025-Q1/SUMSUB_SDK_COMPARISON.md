# Sumsub SDK Comparison: WebSDK vs Mobile SDK

## 📊 Сравнение WebSDK и Mobile SDK

### WebSDK (текущая реализация)

**Что это:**
- JavaScript библиотека, которая загружается в браузере
- Работает через iframe/modal в веб-приложении
- Универсальное решение для всех платформ

**Преимущества:**
✅ **Простая интеграция** - один код для всех платформ
✅ **Быстрое обновление** - Sumsub обновляет SDK на своей стороне
✅ **Не требует сборки приложения** - изменения применяются мгновенно
✅ **Работает на любом устройстве** с браузером
✅ **Меньше размер приложения** - SDK загружается по требованию
✅ **Единая бизнес-логика** - одинаковый flow для desktop и mobile

**Недостатки:**
❌ **Зависимость от интернета** - требуется стабильное соединение
❌ **Ограниченный доступ к нативным API** - камера работает через браузер
❌ **Менее нативный UX** - работает в iframe/modal
❌ **Возможны проблемы с CORS** (как мы видели)
❌ **Меньше контроля** над UI/UX

**Текущая реализация:**
```typescript
// Desktop: Modal с WebSDK
<SumsubWebSDK token={sdkToken} />

// Mobile: QR код → ссылка на websdkLink
const mobileUrl = await fetch('/api/kyc/mobile-link');
<QRCode value={mobileUrl} />
```

---

### Mobile SDK (Native)

**Что это:**
- Нативные библиотеки для iOS (Swift/Objective-C) и Android (Kotlin/Java)
- React Native модуль для гибридных приложений
- Интегрируется напрямую в мобильное приложение

**Преимущества:**
✅ **Нативный UX** - полностью нативный интерфейс
✅ **Прямой доступ к камере/сенсорам** - лучшее качество фото/видео
✅ **Работает офлайн** (частично) - может кешировать данные
✅ **Лучшая производительность** - нативный код быстрее
✅ **Полный контроль над UI** - кастомизация через нативные темы
✅ **Биометрия** - Face ID, Touch ID, отпечатки пальцев
✅ **Меньше проблем с браузером** - нет CORS, iframe issues

**Недостатки:**
❌ **Сложная интеграция** - требуется нативная разработка
❌ **Отдельный код для iOS/Android** - дублирование логики
❌ **Требует сборки приложения** - нельзя обновить "на лету"
❌ **Больше размер приложения** - SDK встроен в APK/IPA
❌ **Обновления через App Store/Google Play** - долгий процесс
❌ **Не подходит для веб-приложения** - только для нативных/гибридных приложений

**Возможная реализация:**
```typescript
// React Native
import { SNSMobileSDK } from '@sumsub/react-native-mobilesdk-module';

SNSMobileSDK.launch(accessToken, {
  onStatusChanged: (event) => {
    console.log('KYC Status:', event.status);
  },
  onError: (error) => {
    console.error('KYC Error:', error);
  }
});
```

---

## 🎯 Что это даст нам?

### Сценарий 1: Веб-приложение (Next.js) - **WebSDK (текущее решение)**

**Рекомендация:** ✅ **Оставить WebSDK**

**Почему:**
- У нас **веб-приложение** на Next.js, а не нативное мобильное приложение
- WebSDK идеально подходит для веб-платформы
- Mobile SDK требует нативного приложения (iOS/Android)
- Мы не можем использовать Mobile SDK в браузере

**Текущая архитектура:**
```
Desktop: Next.js → WebSDK в modal
Mobile:  Next.js → QR код → websdkLink → WebSDK в мобильном браузере
```

---

### Сценарий 2: Если бы у нас было React Native приложение

**Рекомендация:** ✅ **Mobile SDK**

**Почему:**
- Нативный UX для мобильных пользователей
- Лучшее качество фото/видео с камеры
- Поддержка биометрии (Face ID, Touch ID)
- Офлайн возможности

**Архитектура:**
```
iOS App:     React Native → @sumsub/react-native-mobilesdk-module
Android App: React Native → @sumsub/react-native-mobilesdk-module
Web:         Next.js → WebSDK (как сейчас)
```

---

## 📋 Выводы и рекомендации

### Для нашего проекта (Next.js веб-приложение):

✅ **WebSDK - правильный выбор**

**Причины:**
1. **У нас веб-приложение**, а не нативное мобильное
2. **Mobile SDK не работает в браузере** - требует нативного приложения
3. **WebSDK покрывает все наши потребности:**
   - ✅ Desktop verification через modal
   - ✅ Mobile verification через QR код → websdkLink
   - ✅ Единая бизнес-логика
   - ✅ Простая интеграция и обновления

**Текущие проблемы (404 applicant):**
- ❌ **НЕ связаны с выбором SDK** (WebSDK vs Mobile SDK)
- ❌ **Проблема в App Token** - applicant создан с другим токеном
- ✅ **Решение:** Проверить App Token в Sumsub dashboard или пересоздать KYC сессию

---

## 🚀 Когда стоит рассмотреть Mobile SDK?

**Только если:**
1. Мы решим создать **нативное мобильное приложение** (iOS/Android)
2. Или перейдем на **React Native** для мобильных платформ
3. Нужна **биометрическая аутентификация** (Face ID, Touch ID)
4. Требуется **офлайн режим** для KYC

**Для веб-приложения Mobile SDK НЕ применим!**

---

## 📊 Итоговая таблица

| Критерий | WebSDK | Mobile SDK |
|----------|--------|------------|
| **Платформа** | Web (браузер) | iOS/Android (нативное приложение) |
| **Наш случай** | ✅ Подходит | ❌ Не применимо (у нас веб) |
| **Интеграция** | Простая (JS) | Сложная (нативный код) |
| **UX** | Хороший (iframe/modal) | Отличный (нативный) |
| **Камера** | Через браузер | Нативный доступ |
| **Офлайн** | ❌ Нет | ✅ Частично |
| **Обновления** | Мгновенные | Через App Store |
| **Размер** | Не влияет | Увеличивает APK/IPA |
| **Биометрия** | ❌ Нет | ✅ Да |

---

## 🎯 Финальная рекомендация

**Для Apricode Exchange (Next.js веб-приложение):**

✅ **Продолжить использовать WebSDK** - это правильное архитектурное решение

**Текущая проблема (404 applicant):**
- Не связана с выбором SDK
- Связана с App Token или удаленным applicant
- Требует проверки в Sumsub dashboard

**Действия:**
1. ✅ Проверить, что applicant `690e7f5976808036b2e8fa38` существует в Sumsub dashboard
2. ✅ Убедиться, что используется правильный App Token
3. ✅ Если applicant не найден - пересоздать KYC сессию (удалить из БД)
4. ✅ Протестировать полный flow с новым applicant

**Mobile SDK рассматривать только если:**
- Решим создать нативное мобильное приложение в будущем
- Это будет отдельный проект, не замена текущему веб-приложению

