#!/usr/bin/env python3
"""
Скрипт для автоматической расшифровки BCB credentials
Пробует различные пароли
"""

import gnupg
import os
from pathlib import Path

# Инициализируем GPG
gpg = gnupg.GPG()

# Путь к файлу
base_path = Path("/Users/bogdankononenko/Работа/Development/Project/crm vasp/credentials/bcb-sandbox")
gpg_file = base_path / "key" / "Sandbox_credentials.api (4) (1).gpg"

if not gpg_file.exists():
    gpg_file = base_path / "Sandbox_credentials.api (4).gpg"

if not gpg_file.exists():
    print("❌ GPG файл не найден!")
    exit(1)

print("╔═══════════════════════════════════════════════════════════════════════════════╗")
print("║          🔓 PYTHON GPG РАСШИФРОВКА                                           ║")
print("╚═══════════════════════════════════════════════════════════════════════════════╝")
print()
print(f"✅ Файл: {gpg_file.name}")
print(f"📂 Путь: {gpg_file.parent}")
print()
print("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━")
print()
print("🔐 Пробую различные пароли...")
print()

# Список паролей для проверки
passwords = [
    "",
    "Apricode14789230)",
    "Apricode210521)",
    "apricode14789230)",
    "apricode210521)",
    "admin@bitflow.biz",
    "Bitflow2024",
    "Bitflow2025",
    "bitflow",
    "Bitflow",
    "apricode",
    "Apricode",
    "210521",
    "14789230",
]

# Читаем зашифрованный файл
with open(gpg_file, 'rb') as f:
    encrypted_data = f.read()

counter = 1
total = len(passwords)

for password in passwords:
    display_pass = "<пустой>" if password == "" else password
    print(f"[{counter}/{total}] 🔑 {display_pass:<30} ... ", end="", flush=True)
    
    try:
        # Пробуем расшифровать
        decrypted = gpg.decrypt(encrypted_data, passphrase=password)
        
        if decrypted.ok:
            print("✅ SUCCESS!")
            print()
            print("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━")
            print("🎉 НАЙДЕН ПРАВИЛЬНЫЙ ПАРОЛЬ!")
            print("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━")
            print()
            print(f"🔐 Пароль: {display_pass}")
            print()
            print("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━")
            print("📋 CREDENTIALS:")
            print("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━")
            print()
            print(str(decrypted))
            print()
            print("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━")
            
            # Сохраняем
            output_file = base_path / "credentials.txt"
            password_file = base_path / "gpg-password.txt"
            
            with open(output_file, 'w') as f:
                f.write(str(decrypted))
            
            with open(password_file, 'w') as f:
                f.write(f"Пароль GPG: {display_pass}\n")
            
            print()
            print(f"✅ Сохранено в: {output_file}")
            print(f"✅ Пароль в: {password_file}")
            print()
            
            exit(0)
        else:
            print("❌")
    except Exception as e:
        print(f"❌ (ошибка: {str(e)[:30]})")
    
    counter += 1

print()
print("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━")
print("❌ НИ ОДИН ПАРОЛЬ НЕ ПОДОШЁЛ")
print("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━")
print()
print("💡 Попробуйте bash скрипт или напишите BCB Support")
print()


