# 🔔 Notification Events - Как это работает (Русский)

**Дата:** 16 января 2025  
**Статус:** ✅ **Production Ready**

---

## 🎯 Что такое Notification Events?

**Notification Event** — это система для автоматической отправки уведомлений пользователям.

### Простыми словами:

1. **Происходит событие** (пользователь создал заказ)
2. **Система отправляет уведомление** (email + уведомление в приложении)
3. **Пользователь получает** (email на почту + badge в приложении)

### Enterprise подход = создание событий через UI (без кода!)

---

## 📍 Где находится?

```
Админка → System & Configuration → Notification Events
Или прямо: /admin/notification-events
```

**Требуется:** `SUPER_ADMIN` роль

---

## 🏗️ Как это работает?

### Простая схема:

```
Код приложения → eventEmitter.emit('ORDER_CREATED')
                      ↓
               NotificationService (берет настройки из БД)
                      ↓
               Email Template (берет шаблон письма)
                      ↓
               Resend API (отправляет email)
                      ↓
               Пользователь получает ✉️
```

### Компоненты системы:

1. **Notification Event** - триггер (когда отправлять)
2. **Email Template** - шаблон письма (что отправлять)
3. **Notification Queue** - очередь (логирование)
4. **Channels** - каналы (EMAIL, IN_APP, SMS, PUSH)

---

## ✨ Enterprise фичи

### 1. Создание событий через UI (без деплоя!)

**Было (Old Way):**
```typescript
// Нужно менять код и делать deploy
await sendEmail({
  to: user.email,
  subject: 'Order Created',
  html: '<p>Your order...</p>'
});
```

**Стало (Enterprise Way):**
```typescript
// Просто вызываешь событие - всё остальное в БД!
await eventEmitter.emit('ORDER_CREATED', {
  orderId: order.id,
  userId: user.id,
  amount: 100.50
});
```

**Преимущества:**
- ✅ Настройки в UI (не нужен deploy)
- ✅ Шаблоны в БД (можно редактировать)
- ✅ Включить/выключить одной кнопкой
- ✅ Статистика и логи
- ✅ Повторная отправка failed notifications

### 2. Variable Schema (документация для разработчиков)

Когда создаешь событие, указываешь какие переменные нужны:

**Required Variables (обязательные):**
- `orderId` - ID заказа
- `userId` - ID пользователя
- `amount` - сумма

**Optional Variables (опциональные):**
- `couponCode` - код купона (если есть)
- `notes` - заметки

**Example Payload (пример):**
```json
{
  "orderId": "ORD-123",
  "userId": "cm4n123",
  "amount": 150.50,
  "couponCode": "WELCOME10"
}
```

**Зачем?**
- Разработчики видят какие данные нужны
- Можно проверять что всё передано
- Документация прямо в системе

### 3. Multi-Channel Support

Одно событие → несколько каналов:

- ✅ **EMAIL** - отправка писем (работает)
- ✅ **IN_APP** - уведомления в приложении (работает)
- ⚠️ **SMS** - SMS сообщения (не реализовано)
- ⚠️ **PUSH** - push notifications (не реализовано)

### 4. White-Label Support

Email автоматически брендируются:
- Логотип компании
- Название компании
- Цвета бренда
- Support email

Всё берется из `/admin/settings` → Brand Settings

### 5. Статистика

Для каждого события видно:
- Сколько подписчиков
- Сколько отправлено
- Сколько failed
- Когда последнее отправлено

### 6. Bulk Operations

- **Enable All** - включить все события
- **Disable All** - выключить все события

**Use Case:** Maintenance mode - выключить все уведомления

---

## 🚀 Как создать событие? (UI)

### Шаг 1: Создать Email Template

`/admin/email-templates` → `Create Template`

```
Key: MY_EVENT
Name: Мое уведомление
Category: ORDER
Subject: Заказ #{{orderId}} создан
HTML Body:
  <p>Привет {{userName}},</p>
  <p>Ваш заказ <strong>{{orderId}}</strong> создан!</p>
  <p>Сумма: {{amount}} {{currency}}</p>
Variables:
  - orderId: Order ID
  - userName: User Name
  - amount: Amount
  - currency: Currency
Status: PUBLISHED
Active: ✅
```

### Шаг 2: Создать Notification Event

`/admin/notification-events` → `Create Event`

```
Event Key: MY_EVENT
Event Name: Мое событие
Description: Когда пользователь создает заказ
Category: ORDER
Priority: NORMAL
Channels: [EMAIL, IN_APP]
Email Template: "Мое уведомление" (выбрать из списка)

Required Variables:
  - orderId
  - userId
  - userName
  - amount
  - currency

Example Payload:
{
  "orderId": "ORD-123",
  "userId": "cm4n123",
  "userName": "Иван Петров",
  "amount": 100.50,
  "currency": "EUR"
}

Developer Notes:
Triggered in POST /api/orders after order creation

Active: ✅
```

### Шаг 3: Добавить в код

```typescript
// src/app/api/orders/route.ts
import { eventEmitter } from '@/lib/services/event-emitter.service';

export async function POST(request: NextRequest) {
  // ... создание заказа
  
  const order = await prisma.order.create({ /* ... */ });

  // Отправить уведомление
  try {
    await eventEmitter.emit('MY_EVENT', {
      orderId: order.id,
      userId: user.id,
      userName: `${user.profile.firstName} ${user.profile.lastName}`,
      amount: order.totalFiat,
      currency: order.fiatCurrencyCode
    });
    
    console.log('✅ Уведомление отправлено!');
  } catch (error) {
    // Не ломаем создание заказа если уведомление failed
    console.error('❌ Уведомление не отправлено:', error);
  }

  return NextResponse.json({ success: true, order });
}
```

### Шаг 4: Тестирование

1. Создать заказ через API или UI
2. Проверить email пришел на почту
3. Проверить `/admin/notification-queue` → status: SENT
4. Проверить console logs

---

## 📊 Управление через UI

### Включить/Выключить событие

1. Найти событие в списке
2. Переключить switch (ON/OFF)

**Быстро:** Switch OFF → событие перестает работать (уведомления не отправляются)

### Редактировать событие

1. Нажать `⋮` (три точки)
2. `Edit Event`
3. Изменить настройки
4. `Update Event`

**Что можно изменить:**
- ✅ Название
- ✅ Описание
- ✅ Каналы (EMAIL, IN_APP, etc.)
- ✅ Приоритет
- ✅ Email Template
- ✅ Переменные
- ✅ Статус (Active/Inactive)
- ❌ Event Key (нельзя после создания!)

### Просмотр статистики

1. Нажать `⋮` → `View Details`
2. Увидеть:
   - Сколько подписчиков
   - Сколько отправлено
   - Сколько failed
   - Когда последнее

### Удалить событие

1. Нажать `⋮` → `Delete`
2. Подтвердить

**⚠️ Важно:**
- System events (isSystem: true) **нельзя** удалить
- Custom events можно удалить
- Удаление необратимо!

---

## 🎯 Готовые события в системе

### Orders (Заказы):
- `ORDER_CREATED` - Заказ создан
- `ORDER_PAYMENT_RECEIVED` - Оплата получена
- `ORDER_PROCESSING` - Заказ обрабатывается
- `ORDER_COMPLETED` - Заказ выполнен ✅
- `ORDER_CANCELLED` - Заказ отменен
- `ORDER_EXPIRED` - Заказ истек
- `ORDER_FAILED` - Заказ завершился ошибкой
- `ORDER_REFUNDED` - Возврат средств

### KYC (Верификация):
- `KYC_SUBMITTED` - KYC отправлен на проверку
- `KYC_APPROVED` - KYC одобрен ✅
- `KYC_REJECTED` - KYC отклонен ❌

### Security (Безопасность):
- `SECURITY_2FA_ENABLED` - 2FA включен 🔐
- `SECURITY_2FA_DISABLED` - 2FA выключен ⚠️

### User (Пользователь):
- `WELCOME_EMAIL` - Приветственное письмо 👋

### Admin (Админ):
- `ADMIN_INVITED` - Админ приглашен

**Итого:** 15 событий активно, 75% coverage ✅

---

## 🔧 Troubleshooting

### Уведомление не отправляется

**Шаг 1:** Проверь событие активно
- `/admin/notification-events` → Найти событие → Switch должен быть ON

**Шаг 2:** Проверь привязан шаблон
- Событие должно иметь `templateId`
- Шаблон должен быть `PUBLISHED` и `isActive: true`

**Шаг 3:** Проверь Resend API Key
- `/admin/integrations` → Resend → API Key должен быть настроен

**Шаг 4:** Проверь переменные в коде
- Все переменные из шаблона должны быть в `eventEmitter.emit()`

**Шаг 5:** Проверь email пользователя
- У пользователя должен быть валидный email

### Email приходит, но неправильный

**Проверь переменные:**
- В шаблоне: `{{variableName}}`
- В коде: должна быть `variableName` в payload

**Проверь white-label настройки:**
- `/admin/settings` → Brand Settings
- Логотип, название компании должны быть заполнены

### Notification в статусе FAILED

**Посмотреть ошибку:**
- `/admin/notification-queue` → Найти failed → View Details → Смотреть `error`

**Повторить отправку:**
- `/admin/notification-queue` → Выбрать FAILED → `Retry Selected`

**Частые ошибки:**
- `Email template not found` → Привязать шаблон к событию
- `Invalid recipient email` → Проверить email пользователя
- `Resend API error: 429` → Rate limit (подождать)

---

## ✅ Что дает Enterprise подход?

### Для бизнеса:
1. **Быстрые изменения** - не нужен deploy для изменения текста email
2. **A/B тестирование** - можно создать несколько шаблонов
3. **Персонализация** - white-label для каждого клиента
4. **Контроль** - включить/выключить уведомления одной кнопкой
5. **Аналитика** - статистика по каждому событию

### Для разработчиков:
1. **Чистый код** - просто вызываешь событие
2. **Документация** - variable schema прямо в UI
3. **Тестирование** - test endpoint для каждого события
4. **Отладка** - все логи в `/admin/notification-queue`
5. **Масштабирование** - retry logic, queue, multi-channel

### Для пользователей:
1. **Своевременные уведомления** - получают сразу
2. **Красивые email** - брендированные письма
3. **Настройки** - могут отключить что не нужно
4. **Надежность** - retry если не отправилось

---

## 📚 Документация

1. **NOTIFICATION_EVENTS_ENTERPRISE_GUIDE.md** - полный гайд (English)
2. **NOTIFICATION_EVENTS_QUICK_START.md** - быстрый старт с примерами кода
3. **NOTIFICATION_EMAIL_SYSTEM_AUDIT.md** - аудит всей системы
4. **NOTIFICATION_INTEGRATION_COMPLETE.md** - статус интеграции

---

## 🎉 Итог

### ✅ У вас есть:

1. **15 готовых событий** (заказы, KYC, безопасность)
2. **17 email шаблонов** (профессиональные, брендированные)
3. **UI для управления** (создание, редактирование, статистика)
4. **Enterprise функции** (variable schema, multi-channel, retry logic)
5. **Документация** (гайды, примеры, troubleshooting)

### 🚀 Что можно делать:

- ✅ Создавать события через UI (без кода)
- ✅ Редактировать шаблоны на лету (без deploy)
- ✅ Включать/выключать уведомления
- ✅ Смотреть статистику
- ✅ Повторно отправлять failed
- ✅ Настраивать каналы
- ✅ Документировать payload

### 💡 Следующие шаги:

1. Создай свое первое custom событие
2. Протестируй отправку
3. Настрой white-label брендинг
4. Мониторь статистику

---

**Статус:** ✅ **Production Ready!**

**Вопросы?** Смотри полную документацию или создавай issue.

